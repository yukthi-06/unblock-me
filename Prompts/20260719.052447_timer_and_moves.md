> the 'Moves' field, move it to a second bar below the top bar.  Also add a timer to track the time taken to solve the game.    The time has to start on the first move made by user

## Implementation Details
- Added a new `statsLayout` in `activity_game.xml` below the navigation bar.
- Moved the `Moves` field into `statsLayout` and added a new `Time` TextView.
- Implemented a timer in `GameActivity.java` using a `Handler` and `Runnable` that updates the UI every 500ms.
- The timer starts upon the first call to `onMove()`.
- The timer automatically stops when the user wins or resets the game.
