# Show Game Solution Animation

This plan outlines how to integrate an automated solution playback feature into the game.

## Open Questions

Before proceeding, I need clarification on the solution files:
1. **File Naming Convention**: `LevelManager.java` loads levels from `/sdcard/Vypeensoft/Unblock_Me/game/levels/<pack>/<file>.json`. What is the exact path and naming convention for the solution files? (e.g., `/sdcard/Vypeensoft/Unblock_Me/game/solutions/<pack>/<level_id>.txt`?)
2. **File Format**: What is the internal format of these solution files? How are the moves encoded? (e.g., JSON? Plain text with "BlockID Direction Distance"? Please provide an example of a solution file).

## Proposed Changes

### 1. `SolutionManager.java` (New File)
- Create a utility class that checks for the existence of a solution file given a pack and level ID.
- Add a method to parse the file into a List of `Move` objects (once the format is known).

### 2. `activity_game.xml`
#### [MODIFY] [activity_game.xml](file:///x:/Projects_X/0_Active/8_Android_APK/04_Games/00-My-Own/Unblock_Me_APK_GITHUB/app/src/main/res/layout/activity_game.xml)
- Add a new "SHOW SOLUTION" `Button` inside the bottom `LinearLayout`, alongside "UNDO" and "RESTART GAME".
- By default, set its visibility to `gone` or `invisible`.

### 3. `GameActivity.java`
#### [MODIFY] [GameActivity.java](file:///x:/Projects_X/0_Active/8_Android_APK/04_Games/00-My-Own/Unblock_Me_APK_GITHUB/app/src/main/java/com/vypeensoft/unblockme/GameActivity.java)
- In `loadLevel()`, query `SolutionManager` to check if a solution exists for the loaded level.
- If it exists, make the "SHOW SOLUTION" button visible.
- Add a click listener to the "SHOW SOLUTION" button that:
  - Disables user input (or sets a flag).
  - Fetches the parsed solution.
  - Triggers the automated playback sequence on `GameView`.

### 4. `GameView.java`
#### [MODIFY] [GameView.java](file:///x:/Projects_X/0_Active/8_Android_APK/04_Games/00-My-Own/Unblock_Me_APK_GITHUB/app/src/main/java/com/vypeensoft/unblockme/GameView.java)
- Add a `playSolution(List<Move> solutionMoves)` method.
- Use a `Handler` or `AnimatorSet` to sequentially pop a move from the solution list, animate the block moving to its new position, and then trigger the next move until the solution is fully executed and the win condition is met.

## User Review Required
Please provide the file path and format details for the solutions in the Open Questions section, and review the overall plan. Once approved, I will begin implementing the changes!
